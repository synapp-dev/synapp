# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# Dependencies
node_modules
.pnp
.pnp.js

# Local env files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Testing
coverage

# Turbo
.turbo

# Vercel
.vercel

# Build Outputs
.next/
out/
build
dist


# Debug
npm-debug.log*
debug-*.log

# Local scratch / one-off exports (do not commit)
_all_args_strings.txt
_notion_module_overview_body.md

# Misc
.DS_Store
*.pem

# Supabase cross-region migration (secrets + SQL dumps)
scripts/supabase/migration.env
scripts/supabase/artifacts/

# Supersolt: local reference / legacy app copy — do not commit
apps/supersolt/reference/

# CS2 GC bot: persisted Steam refresh token (secret)
apps/intradark/cs2-gc-bot/.steam-session.json

# Jourdain: raw 4K orb source video (150MB, exceeds GitHub limits) - the web asset is jourdain-orb-loop.mp4
apps/jourdain/public/videos/jourdain-orb.mov

# CS2 plugin packaging: download cache, staging tree, and output zips (do not commit)
apps/intradark/.cache/
apps/intradark/dist/
