import { generateMetadataFromSegments } from "@/utils/metadata";

export const metadata = generateMetadataFromSegments(["admin", "lessons"]);

export default function AdminLessonsPage() {
  return <div>AdminLessonsPage</div>;
}
