import { generateMetadataFromSegments } from "@/utils/metadata";

export const metadata = generateMetadataFromSegments(["admin", "classes"]);

export default function AdminClassesPage() {
  return <div>AdminClassesPage</div>;
}
