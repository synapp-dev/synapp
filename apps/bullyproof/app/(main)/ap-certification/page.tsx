import { generateMetadataFromSegments } from "@/utils/metadata";

export const metadata = generateMetadataFromSegments(["ap-certification"]);

export default function APCertificationPage() {
  return <div>APCertificationPage</div>;
}
