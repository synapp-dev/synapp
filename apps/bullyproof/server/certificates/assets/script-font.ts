/**
 * Script font for the certificate recipient name (to match the artwork's
 * Palace Script sample). Populate with a base64-encoded OFL-licensed TTF
 * (e.g. Great Vibes); empty string falls back to Times Italic.
 */
export const SCRIPT_FONT_BASE64 = "";
