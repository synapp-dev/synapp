# Base URL of the Intradark Next app that serves /api/ac/*.
# Production default (baked into the code) is https://intradark.com.
# For local dev against `pnpm --filter intradark dev`, point at the dev server:
VITE_AC_API_BASE=http://localhost:3004
